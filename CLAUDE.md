\# Ludogteka



App interna + portal de clientes para un negocio canino en San Luis Potosí (guardería, hotel, estética).



\## Contexto completo

Antes de trabajar, lee docs/PROYECTO.md: ahí está el detalle de roles, roadmap por fases, reglas de negocio y qué está fuera de alcance.



\## Stack

\- Next.js 16 (App Router, TypeScript, Tailwind), carpeta src/

\- Supabase (Auth, Postgres con RLS, Storage)

\- Esquema por migraciones versionadas: npx supabase migration new <nombre> -> editar .sql -> npx supabase db push

\- Dev en puerto 3001



\## Entornos

\- \*\*Desarrollo\*\* — proyecto Supabase `sgfolltpvktbsiisfuzq` ("Ludogteka"). Es el que queda vinculado al CLI (`npx supabase link`), el que usa `.env.local`, y donde vive todo el residuo de pruebas de las fases 0 a 9.

\- \*\*Producción\*\* — proyecto Supabase `xdsxjhytggpsgrmfuuff` ("ludogteka-prod"), dominio ludogteka.mx, DNS en Hostinger. Sus llaves viven ÚNICAMENTE en las variables de entorno de Vercel — nunca en un archivo del repo, nunca en `.env.local`.

\- \*\*Toda migración se prueba primero en desarrollo.\*\* Solo después de verificarla ahí (REST + navegador) se aplica a producción con `npx supabase db push --db-url <connection string de producción>` — sin re-vincular el CLI, sin tocar el link a desarrollo. Nunca se escribe una migración directo contra producción.

\- \*\*Antes de CUALQUIER despliegue a producción, verificar el estado real de los datos contra la base, nunca contra esta documentación ni contra docs/PROYECTO.md.\*\* Conteos reales de `clientes`, `perros`, `contratos`, `plantillas_contrato`, `cobros`, etc., leídos de producción en ese momento. Los docs describen el estado del día que se escribieron: en Fase 11 decían que producción estaba vacía y ya tenía 14 clientes, 15 perros y el contrato real del negocio publicado — una migración con backfill que se creía inofensiva sí corrió sobre datos reales. Si el conteo no cuadra con lo que esperabas, se para y se revisa antes de migrar.

\- \*\*El despliegue va por `npm run desplegar -- --aplicar`\*\* (`scripts/desplegar-produccion.mjs`), no a mano. El script hace la verificación de datos de arriba, corre el dry-run, aplica las migraciones con reintentos, comprueba que quedaron registradas en la base y SOLO ENTONCES empuja el código. Correrlo primero con `--revisar` (no escribe nada). La cadena de conexión vive en `C:\proyectos\.ludogteka-prod-db` — FUERA del repo, una sola línea con la cadena completa, permanente: \*\*NO se borra al terminar\*\* (decisión del 21 de septiembre de 2026; ya se había dado una vez y se borró, y eso obligó a volver a pedirla). El script la lee de ahí; `LUDOGTEKA_PROD_DB_URL` sigue teniendo prioridad si viene puesta. Nunca se copia al repo, a `.env.local` ni a un mensaje. \*\*El push del código jamás se encadena a la migración con `;`\*\* — en Fase 11 así fue y el `git push` salió con el `db push` fallado: quedó código nuevo empujado contra esquema viejo, y solo no rompió nada porque el build tardó más que el reintento.

\- \*\*Nunca a mano en producción\*\*: nada de SQL manual por consola/dashboard para cambios de esquema (eso es una migración, sin excepción) ni para cargar datos de negocio reales (tarifas, plantilla de contrato, catálogo de insumos, altas de clientes) — esos se capturan por la UI de la app, igual que los va a capturar el negocio en el día a día. La única SQL manual tolerada en producción es de un administrador de la app resolviendo un caso operativo puntual (nunca cambios de esquema), y siempre documentando qué y por qué.

\- \*\*Leer producción sin la contraseña de Postgres\*\*: la cadena de conexión no está en esta máquina a propósito y no hace falta pedirla para VERIFICAR. El CLI de Supabase tiene sesión (token en el Administrador de credenciales de Windows, no en archivo): `node node_modules/supabase/dist/supabase.js projects api-keys --project-ref xdsxjhytggpsgrmfuuff -o json` devuelve las llaves de la API REST, y con la de `service_role` se leen los conteos reales sin escribir nada. OJO: la llave `sb_secret_` que devuelve esa API viene TRUNCADA (41 caracteres) y da `401 Invalid API key`; la que sirve es la `service_role` de tipo `legacy` (JWT). Y ese 401 se disfraza de "la columna no existe" si una sonda por columna solo mira si la petición falló: hay que distinguir el código de estado, o se dan por pendientes migraciones que ya están aplicadas (pasó el 21 de septiembre de 2026). La llave nunca se imprime ni se guarda: se lee al vuelo dentro del script y se usa ahí. Para ESCRIBIR (migrar) sigue haciendo falta `LUDOGTEKA_PROD_DB_URL` y el script de despliegue; la API de gestión también puede correr SQL, pero eso sería SQL manual contra producción, prohibido arriba.



\## Reglas

\- \*\*Commits, migración a producción y despliegue los hace el agente, siempre, sin esperar a que se le pida.\*\* Acordado y reafirmado el 21 de septiembre de 2026. Cuando sus propios controles pasan (migración probada en desarrollo por REST, `tsc` y `eslint` limpios), commitea, corre `npm run desplegar -- --revisar`, y si el estado real de la base cuadra con lo esperado, `-- --aplicar`. No se le pasan comandos al usuario para que los corra él; se corren y se reporta el resultado. Lo único que detiene esa cadena es un control que no pasa o un conteo de producción que no cuadra — y entonces se para y se reporta, no se pregunta "¿procedo?".

\- \*\*Nada espera al servidor sin tope ni spinner.\*\* Todo botón que llame a una server action usa `useEspera()` (`src/hooks/use-espera.ts`): `const envio = useEspera(); const res = await envio.ejecutar(() => accion(...)); if (res.error) ...` y `<Button cargando={envio.cargando}>`. Flujos de varios pasos (subir a Storage) usan `envio.correr(...)`; formularios con `useActionState` envuelven la acción con `useAccionConTope`; un `fetch` de cliente lleva `AbortSignal.timeout(TOPE_MS)`. Nunca `const [x, setX] = useState(false)` + `setX(true)` + `await` + `setX(false)`: así estuvieron 51 botones colgándose para siempre cuando la acción tronaba (23 de septiembre de 2026). El spinner es de `Button cargando`, no un texto quieto.

\- \*\*Todo formulario termina en `<AccionesFormulario error={…} exito={…}>` con sus botones adentro\*\* (`src/components/ui/acciones-formulario.tsx`): la confirmación y el error se ven junto al botón que se apretó, no solo en el encabezado — en un formulario largo el aviso de arriba queda fuera de la pantalla y el usuario vuelve a apretar (23 de septiembre de 2026). El `<Alert>` de arriba se conserva.

\- \*\*Todo link, código o contraseña que se le pase a alguien se muestra con `CampoCopiable`\*\* (`src/components/ui/campo-copiable.tsx`): campo de solo lectura + botón de copiar con "Copiado" visible, respaldo que selecciona el texto si el portapapeles falla, y clic en el campo selecciona todo. Nunca un `<p>` con el link ni un `navigator.clipboard` a mano en el componente.

\- \*\*Un archivo `"use server"` solo exporta funciones async.\*\* Nada de `export type { X }` (re-exportar un tipo importado), `export const`, ni `export {}` de otro módulo. El build de producción de Next deja el re-export como un export en tiempo de ejecución de un nombre que TypeScript ya borró y el módulo entero truena al cargarse (`ReferenceError: MetodoPago is not defined`), con él TODA server action invocada desde la página que lo importe — y en desarrollo funciona, así que no se ve hasta producción. Así estuvo rota la ficha del cliente (link de complemento, restablecer contraseña) del 29 de julio al 23 de septiembre de 2026; el síntoma era el spinner eterno y después el error genérico. Los tipos se importan de donde se definen. Cuando una server action falle en producción con el mensaje genérico, el mensaje trae la referencia (digest): se busca en `npx vercel logs <deployment>`.

\- \*\*Un guardia de rol nunca se escribe contra un valor que pueda ser NULL.\*\* `current_rol()` devolvía NULL para un llamador anónimo, y `null not in ('admin','recepcion')` es NULL, no TRUE: el `if` no se dispara y el guardia deja pasar. Así estuvieron abiertos 39 guardias desde Fase 1 hasta que se encontró en Fase 13. Hoy `current_rol()` devuelve `'anonimo'` y `is_admin()`/`is_staff()` van con `coalesce(..., false)`, así que el idioma de siempre ya es seguro — pero cualquier guardia nuevo se prueba \*\*también con la llave anónima pelada\*\*, no solo con un JWT de cada rol: es el caso que ninguna prueba con sesión puede ver.

\- \*\*`revoke execute ... from public` NO le quita el permiso a `anon` en Supabase.\*\* El proyecto trae ALTER DEFAULT PRIVILEGES que le concede EXECUTE a anon/authenticated/service_role sobre cada función nueva, y eso es una concesión directa al rol: hay que nombrar a `anon` explícitamente para revocárselo.

\- \*\*`capture="environment"` solo en inputs de foto donde el perro está enfrente\*\* (foto de llegada en el check-in, bitácora, foto del perro en la ficha de mostrador). En todo lo que sea documento o comprobante (carnet, comprobante sanitario, contrato en papel) y en todo lo que capture el dueño desde su casa (alta por link, portal) va SIN `capture`: con él, el celular abre solo la cámara y no deja escoger de la galería, donde el dueño ya tiene la foto (23 de septiembre de 2026).

\- \*\*Todo buscador de clientes usa `<BuscadorClientes>`\*\* (`src/components/buscador-clientes.tsx`) con filas de `src/lib/clientes/buscables.ts`: busca por perro, dueño o teléfono y muestra el perro con su dueño. Nunca un `useMemo` con `nombre.includes(q)` a mano en la pantalla: así había seis copias que no sabían buscar por perro (23 de septiembre de 2026). La lista de /clientes en `next dev` no se hidrata (por su `loading.tsx`); se prueba con `next build` + `next start`.

\- Nada de SQL manual por copy-paste: todo cambio de esquema va como migración.

\- RLS obligatorio en toda tabla nueva. Verificar aislamiento con llamadas REST directas, no solo por UI.

\- Español mexicano en toda la UI (tú, no vos).

\- Roles: admin, recepcion, estetica, cliente. El cliente (dueño del perro) nunca ve datos de otros clientes ni información financiera.

\- Fuera de alcance por ahora: facturación CFDI (solo guardar RFC opcional).

\- \*\*Mercado Pago\*\*: `MERCADOPAGO_ACCESS_TOKEN`, `MERCADOPAGO_WEBHOOK_SECRET`, `MERCADOPAGO_TERMINAL_ID` y `LUDOGTEKA_URL_PUBLICA` solo en Vercel / `.env.local`, server-side. Un cobro de Mercado Pago se registra ÚNICAMENTE por `registrar_pago_mercadopago` (idempotente) después de leer el pago/orden de la API con el access token; el webhook rechaza toda notificación sin firma válida y nunca registra con el cuerpo de la notificación. Toda llamada nueva a Mercado Pago pasa por `src/lib/mercadopago/api.ts` (tope y traducción) y tiene rama de simulación (23 de septiembre de 2026).

\- Llaves de servicios externos facturables (Google Maps, etc.) van server-side, SIN prefijo NEXT\_PUBLIC\_ — igual que las de Supabase, en .env.local para desarrollo y en variables de Vercel para producción, nunca en el repo.



\## Checklist: tabla nueva

Cada tabla nueva lleva esto DENTRO de su propia migración de creación, no en un ALTER aparte después — así se perdió una vez (created_by sin default en servicios/tarifas de Fase 3, corregido hasta Fase 4 porque la convención solo vivía en las tablas de Fase 2, no estaba escrita en ningún lado):



\- Columnas estándar: id uuid pk default gen_random_uuid(), created_at timestamptz not null default now(), updated_at timestamptz not null (sin default, lo llena el trigger), deleted_at timestamptz.

\- created_by uuid references auth.users(id) on delete set null default auth.uid() — el DEFAULT va en el propio CREATE TABLE, siempre.

\- Trigger set_updated_at before insert or update on la tabla for each row execute function public.set_updated_at().

\- alter table ... enable row level security, con al menos una política de SELECT y una de INSERT/UPDATE escritas explícitamente en la misma migración. Nunca dejar una tabla con RLS activado y cero políticas (bloquea todo en silencio) ni con RLS desactivado.

\- Baja lógica (deleted_at), nunca DELETE, en cualquier tabla que el negocio necesite dar de baja sin perder el historial.



\## Estado

Fase 0 a Fase 4 completas (auth con roles, vinculación dueño↔negocio, panel de admin, clientes, portal, expediente completo del perro, catálogo de servicios y tarifas, reservas con cupo y calendario, check-in/check-out, cargos, agenda de estética, series recurrentes). Fase 5 (POS: cobros, bonos, descuentos, turno de caja y arqueo) y Fase 6 (contratos: plantillas versionadas, generación y firma en PDF con evidencia de auditoría, subida en papel, visibilidad operativa y vigencia) construidas y probadas con JWTs reales. Fase 5 pendiente de que el negocio la termine de probar para cerrarla formalmente. Fase 7 (inventario) completa (catálogo, movimientos, consumo automático por receta). Fase 8 (reportes) completa (financiero, costos/margen, operativo). Fase 9 (bitácora diaria y medicamentos) completa (fotos/notas/incidencias con aviso por WhatsApp vía wa.me; régimen y registro de dosis administradas). Con esto las diez fases del roadmap original (0 a 9) están completas. Fase 10 (recolección a domicilio: cotizador de distancia por Google Routes API, reutilizando tarifas/cargos_aplicados de Fase 3/4 sin mecanismo de cobro nuevo) completa, corriendo en modo simulación hasta que el negocio capture direcciones reales, tarifas por km y la llave de Google Maps. Fase 11 (varias plantillas de contrato a la vez: `tipos_contrato` con nombre, versionado y aplicabilidad por categoría de servicio propios; el estado de contrato pasó de un sí/no por perro a uno por tipo, y los avisos dicen cuál falta) completa. Fase 12 (navegación: Reservas dividida en los módulos Guardería y Hotel, Agenda renombrada a Estética y movida a /estetica) completa, sin migraciones — `estancias` sigue siendo una sola tabla porque el cupo es del mismo espacio físico, cada módulo filtra por `servicios.categoria` y **la ocupación que muestran los dos es la de toda la casa**. Fase 13 (alta de clientes por link de invitación: recepción manda el link por WhatsApp, el dueño captura sus datos y los de sus perros, y el expediente nace con `profiles.cliente_id` asignado en la misma transacción — sin pasar por la cola de vinculación; el alta manual y /vinculacion se conservan) completa. Fase 14 (precios de estética por grupo de raza: `grupos_raza` + `razas` con búsqueda por alias, `tarifas.grupo_raza_id` como una dimensión más de la misma matriz, y el tamaño decidido por el GRUPO y no por el servicio) completa. Fase 15 (dos flujos de alta por link, guardería/hotel y estética, cada uno con el contrato que le toca firmado dentro del alta, más un link de complemento que solo rellena huecos) completa. Fase 16 (identidad por teléfono: el cliente entra con su número y su contraseña, `clientes.telefono` es único entre los vivos, un número ya registrado no se puede volver a dar de alta y uno sin cuenta se vincula al expediente que ya existe; Auth sigue por correo con una dirección sintética derivada del número —ver `src/lib/auth/identidad.ts`— y las cuentas viejas conservan su correo real porque el login lo RESUELVE en vez de calcularlo) completa. Corrección de estética contra el cartel (10 de septiembre de 2026, `30c089a`): tres servicios de baño —completo, rapado, exprés— con lo que incluye cada uno, pelo maltratado como precio alternativo del baño completo (`tarifas.precio_pelo_maltratado`, marcado en la cita por quien recibe al perro), talla gigante retirada, y \*\*en estética no hay contratos\*\* (`tipos_contrato.categorias_servicio` solo admite guardería y hotel; "categorías vacías" = todo perro que use guardería u hotel, no todo perro). Retiro de los siete servicios de estética de Fase 3 que quedaron vivos sin tarifa (21 de septiembre): vista `servicios_cotizables` (servicio vivo con al menos un precio vigente que no sea `no_aplica`) de la que lee `/estetica/nueva` — \*\*toda pantalla que ofrezca un servicio para agendar o reservar lee de esa vista, no de `servicios`\*\* (estética la usa a secas; guardería y hotel muestran además el que no tiene precio como deshabilitado con aviso, vía `src/lib/servicios/ofrecibles.ts`). Fase 17 (guardería y hotel del cartel, 21 de septiembre de 2026): hotel $270/$300 por talla; guardería ocasional por hora a $35 (`unidad = 'hora'`, `estancias.horas`, cantidad = horas, sube a las reales al check-out); day pass 10/15/20 y mensualidad como bono `ilimitado` (cantidad_total = días hábiles de la vigencia, para que la fórmula de reconocimiento de Fase 5 siga exacta); sin recogida tardía, día extra ni cargo de medicamento — el perro que no recogen pasa a noche de hotel con `convertir_estancia_a_hotel`; requisitos de estancia en `perros` (evaluación de comportamiento con excepción de admin; `en_celo`/`gestante` y alertas con `catalogo_alertas.bloquea_estancia` sin excepción); vigencias bordetella 6 / desparasitación 3 solo para aplicaciones nuevas. `guarderia_dia` a $350 (22 de septiembre; la matriz de guardería/hotel sin celdas vacías). Comida especial es un cargo de \*\*monto libre\*\* (`servicios.monto_libre`): sin celda en la matriz, importe y descripción se capturan al aplicarlo, el importe no se cambia, se cancela con motivo y nunca se borra. \*\*Invitar staff\*\* va por la RPC `asignar_rol_staff` (puerta con nombre `app.asignacion_rol_interna`, solo `service_role`, solo recepción/estética sobre cuentas recién creadas): un UPDATE directo a `profiles.rol` sin sesión de admin lo rechaza el trigger, y así estuvo roto del 10 al 22 de septiembre. Para probar `/api/staff` hay que entrar con cookies (el middleware no acepta Bearer). `/recepcion` es el tablero del día de toda la casa (`src/app/(staff)/tablero-dia.tsx`: llegadas, salidas, adentro, citas de estética, ocupación diurna/nocturna, y "necesita atención" con enlace a donde se resuelve); `/admin` lleva su versión compacta arriba. Day pass desde Guardería: los pases son \*\*del cliente\*\* (cualquiera de sus perros los consume); `aplicar_bono_a_estancia(estancia)` los usa solo al reservar guardería de día completo, eligiendo el que vence primero entre los vigentes EL DÍA DE LA ESTANCIA; venta y saldo en `/guarderia/pases`, aviso en el check-in, sección en el tablero de guardería, y `src/lib/bonos/descripcion.ts` es el único lugar donde se redacta un bono (mensualidad = "activa hasta", nunca saldo). \*\*Cancelar una estancia devuelve el pase\*\* (trigger `devolver_bono_al_cancelar_estancia` → `devolver_bono_de_item`: fila `devolucion` con motivo y quién, nada se borra; bono vencido no devuelve y lo dice; "no llegó" no devuelve). \*\*La cobertura de una línea siempre es consumos − devoluciones\*\* (`cobertura_bono_de_item`); sumar solo consumos cuenta pases ya devueltos. Las series aplican el pase al generar cada estancia (`generar_estancias_serie` devuelve `bono jsonb` por fecha; sin saldo queda día suelto sin abortar). \*\*El link de alta/complemento vive hasta que el cliente termine TODO\*\* (23 de septiembre): `invitaciones_cliente.alta_completada_at` = ya guardó sus datos (estado `en_curso`, "Registrado, falta firmar"), `usada_at` = no queda nada, ni firmas; un link en curso no vence, al reabrirlo se reconoce al dueño (contraseña por teléfono) y `completar_expediente_cliente` devuelve TODOS los contratos del flujo pendientes (no solo los nuevos); `cerrar_invitacion_si_completa` lo cierra con la última firma y la pantalla de un link cumplido manda al portal. \*\*Comprobantes sanitarios desde el portal\*\* (23 de septiembre): el dueño sube la foto del carnet con tipo y fecha y queda en `requisitos_sanitarios_propuestos` como PROPUESTA — no toca `requisitos_sanitarios_aplicados` ni la vista de estado, así que no levanta el bloqueo de reserva; recepción la confirma o rechaza con motivo en `/recepcion/comprobantes` (`revisar_requisito_propuesto`), y al confirmar se inserta la aplicación real con la vigencia del catálogo y la misma foto. La foto la sube el servidor con la secret key tras comprobar que es el dueño principal; el dueño no tiene escritura en Storage. \*\*Foto del perro desde el portal\*\* (23 de septiembre): el dueño principal la sube o reemplaza directo —es identificación, no documento— vía `subirFotoMiPerro` + RPC `actualizar_foto_mi_perro` (dueño principal, perro vivo y no fallecido, ruta dentro de `{cliente}/{perro}/perfil/`); `PerroFoto` tiene `subirConAccion` y `capture` para ese camino. \*\*Fase 18\*\* (23 de septiembre): Caja es el mostrador (`/caja`: cuentas abiertas con saldo, buscador compartido, cargo suelto sin reserva vía `crear_cargo_suelto` —`cargos_aplicados.reserva_id`, `estancia_id` opcional—, pases; `/caja/cobrar/[reservaId]` es la MISMA `PantallaCobro` que `/reservas/[id]/cobrar`; `/caja/turno`: turno, retiros, arqueo y `movimientos_turno`/`resumen_turno` por método y origen). \*\*Mercado Pago\*\*: terminal Point por la API de Orders (`POST /v1/orders type=point`, la vigente desde 2025) y links por Checkout Pro; `mp_ordenes` + `registrar_pago_mercadopago` (solo `service_role`, idempotente: una orden = un cobro; `cobros.origen` distingue manual / mercadopago_point / mercadopago_link); webhook en `/api/mercadopago/webhook` con firma HMAC obligatoria y lectura del recurso por API, nunca del cuerpo; sin `MERCADOPAGO_ACCESS_TOKEN` corre en simulación. Guía para el negocio en `docs/MERCADO-PAGO.md`. \*\*Los requisitos sanitarios solo se reclaman a perros que usan guardería u hotel\*\* (vista `perros_con_guarderia_hotel`: estancia de esas categorías o contrato vivo); un perro solo de estética ve una nota neutra en portal, expediente y ficha del cliente, y puede adelantar sus comprobantes. El bloqueo al reservar no cambia. Detalle en docs/PROYECTO.md.

